# Runtime Edge API address. Never place staff passwords, access tokens, device keys, or PSP
# credentials in VITE_* variables because Vite embeds them in the browser bundle.
VITE_API_URL=http://127.0.0.1:8000/api/v1
