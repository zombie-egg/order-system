# Windows production boundary

Commercial deployment will later package the Edge API, Worker, and Windows Agent as managed Windows Services and run the Kiosk in Microsoft Edge Kiosk mode or a minimal WebView2 host.

Phase 2 does not install services, change firewall rules, or lock down Windows.
